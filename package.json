{
  "name": "reed-web-demo",
  "private": true,
  "version": "0.1.0",
  "description": "Browser demo for the reed-web npm package — hold the mic, speak, release, clean text comes back.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "license": "MIT",
  "dependencies": {
    "reed-web": "^0.1.0"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
